# `run_lua` 能力测试报告（lua-wasm 原生引擎）

> 针对新工具 `run_lua`（Lua 5.4 → WebAssembly）的能力验证矩阵。
> 背景：bash 沙箱此前明确禁止执行任意代码（`NO code execution`）。`run_lua` 是**唯一允许的执行能力**——在浏览器内存中运行真正的 Lua 解释器，做纯计算；引擎边界保证它不改文件、不联网、不持久化。
> 结果列在 **部署后**（GitHub Pages 链接，应用内直接调用 run_lua）逐条实测填写。

## 记录表

| 编号 | 要验证的能力 | 预期结果 | 实际结果 | 是否通过 |
|------|--------------|----------|----------|----------|
| 1    | 基础算术 | `print(6*7)` → `42` | 部署后实测 | ⏳ |
| 2    | 字符串拼接 | `print("a".."b"..3)` → `ab3` | 部署后实测 | ⏳ |
| 3    | 字符串格式化 | `string.format("%.2f", 3.14159)` → `3.14` | 部署后实测 | ⏳ |
| 4    | Lua 模式匹配 | `("hello 123"):match("(%d+)")` → `123` | 部署后实测 | ⏳ |
| 5    | gsub 替换 | `("a1b2"):gsub("%d","#")` → `a#b#` | 部署后实测 | ⏳ |
| 6    | 表 / 数组 | `local t={1,2,3} print(#t)` → `3` | 部署后实测 | ⏳ |
| 7    | 表 / 字典 + 遍历 | `for k,v in pairs({a=1}) do print(k,v) end` → `a 1` | 部署后实测 | ⏳ |
| 8    | 嵌套表 | 嵌套结构组装/取值 | 部署后实测 | ⏳ |
| 9    | 循环/条件 | `for i=1,3 do ... end` / `if ... else` | 部署后实测 | ⏳ |
| 10   | 函数 + 闭包 | 自定义函数、upvalue | 部署后实测 | ⏳ |
| 11   | stdin `io.read("*a")` | 读取 `input` 全部内容 | 部署后实测 | ⏳ |
| 12   | stdin `io.lines()` | 逐行处理 `input` | 部署后实测 | ⏳ |
| 13   | `io.open(arg[1])` | 以文件形式读取同一份 `input` | 部署后实测 | ⏳ |
| 14   | input 分组聚合 | 对多行数字求和/计数/分组 | 部署后实测 | ⏳ |
| 15   | 语法错误 | stderr 返回 `lua: ...` 错误信息，ok:false | 部署后实测 | ⏳ |
| 16   | 运行期错误 | 除零/索引 nil → 报错含 traceback | 部署后实测 | ⏳ |
| 17   | 大输出截断 | 超 20k 字符给截断提示 | 部署后实测 | ⏳ |
| 18   | 空脚本 | `run_lua({script:""})` → 提示 missing script | 部署后实测 | ⏳ |
| 19   | 无 input 时 arg[1] | 脚本不传 input 时 io.open(arg[1]) 不可用但 io.read 空 | 部署后实测 | ⏳ |
| 20   | 权限边界：VFS 只读 | 脚本内访问文件/路径 → 无此能力（报错或不可达） | 部署后实测 | ⏳ |
| 21   | 权限边界：无网络 | 脚本内发起请求 → 无此能力 | 部署后实测 | ⏳ |
| 22   | Plan 模式可用性 | run_lua 在 Plan 模式可调用（纯计算非变更） | 部署后实测 | ⏳ |
| 23   | 降级路径 | wasm 未加载时返回「原生引擎不可用」而非假执行 | 部署后实测 | ⏳ |

## 测试结论

**（部署后填写）**

### ✅ 说明

- **安全边界是引擎级强制**：wasm 内的 Lua 只有 emscripten 给的 stdin/stdout/stdin 文件注入，
  没有 VFS、没有 DOM、没有网络 API——「不改文件/不联网/不持久化」不是提示词建议，而是物理不可达。
- 本地 `bun dev`（未装 emsdk）时 run_lua 走降级路径（诚实报「原生引擎不可用」），
  以上矩阵反映的是 **部署版（CI 构建的 wasm）** 的行为。
- 简单行列处理（取列、求和、替换）仍应使用 bash awk/sed；run_lua 服务于 awk 表达不了的复杂逻辑。

## 何时不要用 run_lua

- 取第 N 列 / 替换文本 / 行号过滤 → 用 bash `awk` / `sed` / `cut`
- 计算器级算术 → 用 bash `bc`
- 任何「改文件、跑构建、装包、联网」的诉求 → 一律不可行，换用 read/write/edit 工具按步完成
