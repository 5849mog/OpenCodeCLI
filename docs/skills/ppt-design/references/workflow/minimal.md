# 最小任务流程

适用：单页，文案已经给出，且用户点名风格、给参考图或明确“你决定”。

1. 运行 `doctor.py --json`；`blocked` 时先停下修环境。
2. 用 3–5 行确认卡确认：核心文案、视觉方向、场合、是否动画、不可改边界。
3. 读目标风格章节、`pptx-core/SKILL.md` 与 `scripts/skeleton.js`；有素材/公式/插图/动画
   才加载对应文件头。
4. 施工后运行 `qa.py`，再渲染并做视觉验收；有 `warnings`/检查降级，交付时如实声明。

单页可以压缩沟通，不能跳过环境裁决、确认和两道闸门。
