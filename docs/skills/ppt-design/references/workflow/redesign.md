# 现有 PPT 重设计流程

适用：用户提供现成 `.pptx`，要求整体换风格、重排、重构叙事。

1. 先用 python-pptx 提取每页文字为大纲，请用户确认“内容不变”范围。
2. 默认不改原标题；用户明确允许改文案后，才将标题改为判断句。
3. 运行 `doctor.py --json`，再使用用户方向确认卡确认新的主张、视觉方向和边界。
4. 生成完整内部施工单，按 `workflow/standard.md` 的样板页、两道闸门和交付流程施工。

仅填模板或只改错字/数据时，改走 `pptx-core/SKILL.md` 对应章节。
