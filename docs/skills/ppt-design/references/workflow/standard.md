---
name: ppt-design
description: >
  做 PPT / 幻灯片 / 演示文稿 / pptx / deck 时的设计风格层。流程是：先一轮问清需求，输出风格规格书待用户确认，再按确认的风格施工，最后过两道闸门（程序化检查 + 渲染视觉验收）确保不静默翻车。
  只要用户要做 PPT、幻灯片、演示文稿、slides、deck、汇报页、封面页、章节页、单页介绍页、路演 / 答辩 / 提案材料，或提到「好看 / 有设计感 / 某种风格 / 别太模板化 / 别一眼看出是 AI 做的」，就使用本技能——即使用户没提「设计」二字；要把现有 PPT 整体重设计或换风格，也归本技能。
  本技能只管风格与流程，不含 pptxgenjs API。API 与代码写法在自带的 `references/pptx-core/SKILL.md`（本技能唯一的 pptx 参考，不依赖任何外部技能；来源与改动记录见同目录 PROVENANCE.md）。
  例外：用户提供了自己的 .pptx 模板要往里填内容，或只是改现有 PPT 的错字/数据，走 `references/pptx-core/SKILL.md` 里的模板填充 / 编辑现有 pptx 流程，本技能不覆盖那两条流程。
---

# 常规多页 deck 完整施工流程

> 本文件由顶层 `SKILL.md` 按任务路由加载，保存原有的细节规范。顶层协议和
> `references/environment.md` 优先：先运行 `doctor.py --json`，先给用户方向确认卡，
> 再在内部生成完整施工单。不要把下文完整规格书默认抛给非专业用户。

顶层协议要求的用户确认卡见 `references/user-confirmation-card.md`；本文件的“风格规格书”
现在是内部施工单的详细内容，只有用户明确要求详细方案或需审批时才展示。

## 定位

自带的 `references/pptx-core/SKILL.md`（本技能唯一的 pptx 参考）告诉你**怎么用 pptxgenjs 画出一个 pptx**。本技能补它没有的三件事：

1. **风格库** —— 可直接执行的完整预设（精确色值、字体配对、字号体系、母题、专属禁忌），而不是「选个模板」。
2. **沟通协议** —— 问清需求、出规格书、拿到确认再动手，避免做完十页才发现方向错。
3. **静默翻车清单** —— 一批不会报错、文件正常生成、但成品已经坏掉的坑（库级 bug、字体被替换、中文断错词）。

**开工前先读 `references/pptx-core/SKILL.md`**（本技能唯一的 pptx 参考，无需任何外部技能），API、坐标、图表、模板继承那些写法都在它那儿，本技能不重复。**它已按本技能规则对齐**；如仍有出入，以本技能为准：默认值看规格书，设计看 `references/styles.md`，坑看 `references/pitfalls.md`，动效手法看 `references/anim-atlas.md`（取材库）——来源与改动记录见 `references/pptx-core/PROVENANCE.md`。

**动画与切换**：pptxgenjs 画不了动画，本技能用后处理注入补上（`scripts/inject_anim.py`，菜单制——你写中文动画脚本，工具编译成 PowerPoint 权威格式并自动验证，你不碰 XML）。**分档点是「有没有人在现场讲」，不是「想不想炫」**：

- **有人现场讲**（汇报/教学/路演——绝大多数）→ 动画的任务是"指出现在讲的是这个"，所以**按节拍分组揭示**：一组 = 一句话能讲完的一个论点单元；一次揭示出几个、这一页共几组，由你按该页论证判断，写进规格书让用户过一眼。分组的写法就是 `触发` 的语义：**写 `点击`（或 `自动`）＝开新组，写 `之后`（或 `同时`）＝并进上一组**——"一次点击出几个"就是"这一组里放几条"。qa 只做**护栏**（一页 >4 组、单对象成组、单节拍 >8s 告警，不预设数字）。
- **没人讲**（炫技/展映/大屏循环/当视频看，或纯视觉呈现页——用户明确说是为了效果）→ **上面那套不适用**：长动画、组合动画、一页多组、整页自动播完都是想要的，节拍护栏的阈值不该拿来当尺子（`--palette`、`--min-pt` 这类也不一定适用）。**但豁免必须写明**：规格书里注明"这份/这页是炫技或展映，不接受节拍约束"，交付说明里列出你**明知而保留**的那些告警。qa 输出的节拍条目全是**告警不是硬伤**，不挡交付——**不要默不作声地放过，也不要为了让报告干净而把动画改弱或拆掉**（那是把用户的炫技需求改成平庸）。

**唯一的动效质量参数是缓动**（`线性/缓入/缓出/缓入缓出`，默认缓入缓出）——它不改效果名，只改运动曲线，线性运动是机械/廉价观感的主要来源；一般不用写，走默认。**动画决策必须摆到台面上**：每份规格书都有一行「动画」，写清"有没有人现场讲"、页型与分组（要了演讲备注的，把节拍一并写进备注）；自读/打印场合写明「不加」（负资产）；写「无」也要写出来，不许静默略过这一项。

**插图**：pptxgenjs 只会画方块和文字，画不了函数图像和几何题图。本技能同样用后处理注入补上（`scripts/inject_figure.py`）：你写中文声明式规格（点/线段/圆/弧/角标记/直角/等长/平行/函数曲线/阴影/标注），工具编译成 600dpi 透明图按锚点占位框落位，并**逐条验算你自己声明的几何约束**（等长/直角/平行/共线/中点/圆过点/角度/长度）——算错坐标会被当场拦下，不会静默交出一张与题面矛盾的图。**先写作图依据再出规格**：把读到的已知条件与作图意图用文字说清，这张图才有可对照的基准。词表覆盖不到的怪图有「自定义代码」逃生舱兜底（会标注「未受约束验算」）。考试卷/书页场景**默认裁切清洗优先**（`prep_assets.py` 的「扫描件」：白底归一 / 去斜 / 裁白边 / 对比），重绘只在需要重排、拆成多步、统一标签或做动画时才用。

**改稿场景**：用户给了现成 pptx 要整体重设计或换风格——先用 python-pptx 把每页文字提取成大纲请用户过目，然后照常走三阶段（阶段一第 3 问「内容来源」就此有了着落）。改稿默认**不动原标题**——「内容不变」优先于判断句标题规则；用户允许改文案时在规格书注明。

---

## 三阶段铁律

不要跳过阶段二直接写代码。**方向错了在第二阶段花三十秒能拦住，做完十页再改要重来。**

| 阶段 | 产出 | 是否需要用户确认 |
|---|---|---|
| 一 · 问清需求 | 一轮 AskUserQuestion | 用户点选即可 |
| 二 · 方向确认卡 + 内部施工单 | 用户一屏确认卡；AI 内部结构化施工单 | **必须等用户确认卡通过** |
| 三 · 施工 | pptx + 预览图 | 过两道闸门后交付 |

**极小任务**（单页 + 文案已给 + 风格已点名）：阶段一可跳，规格书可压到三五行（配色 / 字体字号 / 母题 / 动画各一行），但确认与两道闸门一样不省。

---

## 阶段一 · 问清需求（一轮问完）

用 **一次** `AskUserQuestion`（最多 4 问）收齐，不要连环追问。

**先判断能不能跳过。** 以下情况直接进阶段二，再问就是啰嗦：

- 用户已经点名了风格，且只要一两页（例：「做一页新粗野主义的介绍页」）
- 用户给了参考图或参考链接 —— 参考图携带的信息比任何形容词都多
- 用户明确说「你替我定」「随便，你决定」

信息缺得多时，问这四项：

1. **场合与受众** —— 投屏演讲 / 发出去让人自己读 / 打印。这决定最小字号和每页信息密度：投屏要大字少字，自读可以密。
2. **要什么效果** —— 先问效果（舒服 / 眼前一亮 / 专业可信），**再**给匹配的风格预设。`references/styles.md` 第三节有「按效果选风格」表——用户的答案比你的审美有效得多。选项里每个预设都要后缀「来源 + 效果」，例：「蓝图纸工程风（眼前一亮 · Herschel 蓝晒 1842）」。允许收参考图（参考图 > 任何形容词）。
3. **篇幅与内容来源** —— 几页；文案由用户给还是你写；有没有数据要出图表。
4. **硬约束** —— 品牌色 / 指定字体 / logo / 中英文 / 是否需要演讲备注 / **有没有图片素材或要用的图标**（素材放工作目录 `assets/` 或给路径；我们会先清点回报、逐张确认处理方式，不自作主张改图）/ **有没有考试卷、书页的扫描件要放进来**（扫描图先做白底/去斜/裁白边，还是按题重画成干净图，清点后逐张确认）。

动画不在这四问里单独立项——它的决策点**固定在规格书的「动画」行**：写清推荐或不加的理由，随规格书一起被确认，用户点头即拍板（见阶段二模板）。

**问法要点：** 每个选项写清「选它意味着什么」，并把你推荐的放第一个标注（推荐）。风格类问题适合用 `preview` 字段放小样对比。用户对某项答不上来、说「随便」或「不知道」时，给一个带「（推荐）」的默认可选项让他点选，不要跳过问题或干等；他仍不定就按推荐走，并在规格书「已知取舍」里注明「<某项> 按推荐默认」。

---

## 阶段一点五 · 先把论点理顺，再动版式

**排版解决不了"没话说"。** 十页各自漂亮但没有论证推进，读者只会觉得"看着挺像，但记不住"——这种空转感是 AI 味的一大来源，而且任何配色都救不了。

动笔排版之前先写清两件事（写在规格书里，不用另起文档）：

1. **一句主张** —— 这份 deck 要让人接受的那**一个**判断。不是主题名。「Transformer 是什么」是主题；「Transformer 的本质是把'逐字读'换成'一眼看全'」是主张。
2. **3–5 个支撑论点** —— 每个论点是主张的一条腿，不是主题的一个子目录。

然后给每页派一个**论证角色**：立论 / 举证 / 转折 / 收束。规则很硬：

> **一页说不出它在论证里干什么，就该删掉或与相邻页合并。**

这条能直接拦住"十页全是并列地解释概念"——那不是论证，是词条表。

**再把主张下沉到页标题。** 每页标题写成一句判断，不写路标词：「逐字读有三个代价」在推进论证，「三个代价」只是目录。名词短语标题（「市场分析」「产品优势」「背景介绍」）和"十页并列解释概念"是同一种病。文案由你写时，每页标题（判断句）进编排表，随规格书一起被确认。

顺带一个有用的区分：**「Keynote 风格」是论证方式的请求，不是视觉风格的请求**。用户说想要"发布会那种感觉"，要的是"一个主张 + 大尺度视觉 + 揭示节奏"这种推进方式，配任何视觉风格都成立，不要去翻风格库找一个叫 keynote 的皮肤。

常见的几种论证推进方式，供定角色时参考（选一种为主，不要混）：**结论先行**（先给判断再举证，适合决策汇报）、**故事弧**（处境 → 张力 → 化解，适合提案与案例）、**逐步分解**（拆概念、讲步骤，适合教学）、**视觉主导**（大图大数字带节奏，适合发布）、**平铺速查**（等权罗列、不下判断，适合状态同步与手册）。

---

## 阶段二 · 内部施工单（必须先等方向确认卡通过）

**出规格书前必读 `references/styles.md`**：预设的精确色值字号从这里取；第三节「按效果选风格」支撑阶段一第 2 问，第六节「页级编排规则」支撑本节的编排表——冲突以它为准。

先按 `references/user-confirmation-card.md` 输出用户方向确认卡，然后停下等用户回话。
**不要把下面完整施工单默认展示给用户。**确认通过后，按以下字段在内部一次写完；
用户明确要详细方案、或任务涉及审批时才展示对应部分。

```
## 内部施工单

**先看这里**：这份给内部评审看，要让他们一眼觉得「这是工程师做的东西」；主色是深蓝底 + 白线，整体像一张工程蓝图。
**一句主张**：Transformer 的本质是把「逐字读」换成「一眼看全」
**支撑论点**：① 逐字读的三个代价 ② 一眼看全需要的三块基石 ③ 它撑起了今天的大模型
**风格**：蓝图纸工程风
**风格来源**：Herschel 蓝晒工艺（约 1842）+ 20 世纪工程蓝图传统 —— 无来源的风格不许出现在这里
**效果承诺**：眼前一亮（技术主题的意外感；本页翻开的 1 秒落点是蓝图标题栏）
**场景判断**：投屏 · 12 人内部评审 · 最小字号 14pt
**配色**（参考调色，本次直接沿用；如按主题重推需注明取色依据）
  BG    1D4E89   蓝图蓝
  LINE  DCE7EF   白线：图框/网格/正文
  STAMP C13B2E   印泥红：唯一强调
**用色纪律**：3 个色相（底/线/点缀），底占 65–75%，点缀 ≤2% 且只给戳记与关键路径
**字体**：黑体（标题）+ 微软雅黑（正文）+ Arial（拉丁）—— 均在跨平台安全名单内
**字号体系**：主标 56 / 栏标 18 / 正文 15 / 图注 12
**版式结构**：时间轴（每页结构不连续重复，规则见 styles.md §六）
**视觉母题**（每页复现）：双线工程图框 + 右下角标题栏 + 一枚印泥红戳
**动画**：无——内部评审逐页讨论，动画只会拖节奏。（此行每份规格书必填。**先答一个问题：这份有没有人在现场讲？** 有人讲 → 写「按节拍揭示：P3 三组（①工程图框 ②三个问题 ③结论块），点击 3 次，每组 2–3 个对象」——**分组由你按该页论证判断**，在这里列明，用户点头即定；超过 4 组的页写明理由。没人讲（炫技/展映/用户明确要效果）→ 写「P1/P8 整页自动播完，长动画与多组都要，不接受节拍约束」并列出你明知而保留的告警。要了演讲备注的，把这套节拍同时写进备注，演讲者视图就能替你记住）
**图标**：不用／用 Tabler 线性，单一颜色角色（如全 INK，关键页一处 ACCENT），落点：P2 三张能力卡上方各一个，共 3 个（纪律：每页 ≤3、禁 emoji、禁「图标+圆角卡片+投影」三件套；不用的场合写「不用」）
**素材**：无／逐张声明处理方式与出处，例：「产品照.jpg → 裁切 16:9、双色调 INK+ACCENT，放 P3 右半；出处：用户提供」（未经确认不得改动素材；外部素材必须能说出处）
**插图**：无／逐张写作图依据（已知 → 作图意图 → 用哪些图元），例：「P3 二次函数 y=x²−2x−3：标顶点与 x 轴交点、阴影区间 [−1,3]；P4 直角三角形 ABC：∠A=90°、AB=4、AC=3，作斜边高 CH」。几何关系写进规格的「约束」让工具验算；扫描件写明走「裁切清洗」还是「重绘」（默认清洗优先），声明式规格与图元表见 `scripts/inject_figure.py` 文档头

**逐页编排表**（多页必填，格式必须逐页落定，不许只写"内容页×8"；页标题列在文案由你写时必填）

| 页 | 论证角色 | 页标题（判断句） | 版式结构 | 焦点元素 | 视觉载体 | 母题落点 |
|---|---|---|---|---|---|---|
| P1 | 立论 | 一眼看全，正在取代逐字读 | 单点聚焦 | 一句话主张 | 大数字 | 图框+标题栏 |
| P2 | 举证 | 逐字读正在付出三个代价 | 左右对分 | 对比箭头 | 流程示意 | 图框+红戳 |
| P3 | 转折 | 一眼看全靠三块基石撑起 | 时间轴 | 通高时间线 | 流程示意 | 图框+标题栏 |
| P4 | 收束 | 注意力已进入一眼看全的时代 | 单点聚焦 | 底部大结论 | 大数字 | 图框+红戳 |
| … | | | | | | |

编排规则（写表时逐条核；完整版见 styles.md §六）：
- 每页有论证角色 + 一句判断式标题；说不出角色的页删掉或合并
- 版式不连续重复、同结构 ≤ 全部页的 1/5、每页一个焦点、视觉载体（图/图表/示意/大数字）轮换；**强调色预算按页算**——某页已有红色焦点元素（公式图、图表强调）时，母题里的红戳要么省略要么换中性色，一页不能有两个红点
- 页眉页脚只准纯文字或 1px 细线（结构组件例外见 pitfalls #17）；封面/结束页互为独立设计，结束页收束观点

**已知取舍**：这个风格在金融、医疗、政府类场合会显得不稳重；如果受众保守，建议换深色高级感。

—— 用户方向确认卡通过后开工；详细字段仅在需要时与用户复核。
```

规格书里的每一项都必须是**可执行的具体值**，不能写「高级灰」「现代感」这种无法落地的词。判据：把这套配色挪到一个完全无关的主题上会不出戏，才算够具体。

**首行「先看这里」是给非设计背景的用户看的**：用大白话写清「给谁看、要什么效果、主色和整体意象」，一到两句，别复制下面的术语（配色纪律、母题落点之类留在字段里）。用户往往只扫这一行就回话，它决定方向对不对。

阶段一第 4 问收集过的**演讲备注**在这里落地：用户要了，编排表就加一列「备注」，每页写一句「讲这页时说这句话」——不是把页面文字复述一遍。

---

## 阶段三 · 施工

按顺序做，不要跳步。

1. **探工具链** —— node、pptxgenjs 是否装好；渲染器用哪个（见 `scripts/render.py`）。缺什么先装，别等到要出图时才发现没有渲染器，那时你就只能盲写。
2. **核对字体真实存在** —— 列本机字体，确认规格书里点名的字体确实在。**写进文件的字体名如果本机/目标机没有，会被静默替换，字宽一变排版就散，而且预览时看不出来。** 做法见 `references/pitfalls.md` 第 1 条。
3. **把栅格与调色板写成常量** —— 画布宽高、页边距、栏宽、间距、全部色值都在脚本顶部定义，正文里只引用常量。多个模块的右边缘要锁同一个常量，否则会出现 0.05 英寸的错位。
4. **写代码前先读 `references/pitfalls.md`** —— 静默翻车清单，这里每一条都是「不报错但成品已坏」，凭经验绕不过去。然后从 `scripts/skeleton.js` 起手，它已内置绕过库 bug 的写法。要了演讲备注的：每页排版落定就 `slide.addNotes('…')` 写进文件——备注两道闸门都覆盖不到（qa 不读、judge 只看渲染图），交付前自己通读一遍。
5. **素材调理（有图片素材才做）** —— 素材放工作目录 `assets/`，成品放 `assets/_ready/`，生成脚本只引用 `_ready/`。先跑清点模式回报用户（`python scripts/prep_assets.py assets/` 打印尺寸/比例/问题），**逐张确认用途与处理方式后再处理**（`prep_assets.py assets/ spec.json`）：默认只做无损项（EXIF 转正 / sRGB / 按版面比例裁切 / 降采样到 300PPI）；双色调、灰度、颗粒是要了才做。**手机拍的卷子/书页**在规格里加 `"扫描件": true`——白底归一 → Otsu 去斜 → 裁白边 → 对比（可选 `去噪`），专治灰底、歪斜、白边、噪点；**默认裁切清洗优先、重绘按需**。**不要自作主张改用户素材**，图片出处记进交付说明。裁切会切掉原图内容——比例与锚点要写进回报让用户知道；素材图上自带的文字，图注不要复述（避免重复）；用户说「别改太多」时保留原色，并在规格书「已知取舍」里写明照片与风格可能的色彩反差。
6. **样板页确认（多页必做）** —— 先只做 P2（第一张内容页），渲染给用户看（样板在整稿里按最终页序构建时用 `render.py deck.pptx sample --pages 2` 只出这一张；样板单独成文件则 `--pages 1`）：风格、焦距、页眉页脚形态就长这样，确认后继续铺 P1 和 P3–Pn。这一张的成本是渲染一次，10 页全错的重做成本是全部。用户样板页说「换」，此时只改一页。样板页若含公式、插图或图标，先对样板做一次对应注入再渲染（步骤 7/8/9 的命令对样板同样适用），让用户看到的样板与成品一致。
7. **公式（有公式才做）** —— LaTeX 用 `$$...$$` 包裹并**独占一个文本框**（字号、颜色、位置都继承它），`python scripts/inject_math.py deck.pptx` 把它渲染成 600dpi 透明图放回原位——WPS / 真 PowerPoint / Keynote 观感一致；`--dpi 1200` 再提清晰度，`--fontset stix` 换公式字体。行内 `$...$` 不支持（图片进不了文字流）——报错时把式子改成独立文本框。mathtext 覆盖分数/根号/上下标/希腊字母/求和/积分/极限；矩阵、cases、多行 align 不支持。公式是图片：注入后文本框被替换，形状名变成 `formula:<式子前 24 字>`——要给它挂动画就按这个名字引用，并**先注公式再注动画**（颠倒顺序会留下悬空 id，qa 判硬伤）。公式图不进 qa 的文字对比度检查，但会按 `formula:*` 命名做**图形对比度兜底**（<2.5 判硬伤）——深底上公式要用亮色（如线色）或加浅底托板，别把红公式直接压深蓝底（pitfalls #24）。
8. **插图（规格书「插图」行声明了才做）** —— **先把作图依据写成文字**（已知条件 / 作图意图 / 用哪些图元），再写声明式规格：`python scripts/inject_figure.py deck.pptx figures.json --colors "INK=111111,ACCENT=E4002B"`。函数图像走 `坐标轴/函数/阴影`，平面几何走 `点/线段/圆/弧/角标记/直角/等长/平行/标注`；点先命名，线段等按名字引用，坐标只写一次。**几何关系必须写进「约束」**（等长/直角/平行/共线/中点/圆过点/角度/长度）——工具逐条验算，超差即报错中止、文件不动，按实测值改坐标再跑；确实要照画加 `--force`（会大声警告）。`适配`（适应/充满宽度/拉伸）把图放进锚点占位框；图自动跟随锚点文字色，或用 `--colors` 指定角色。词表覆盖不到的怪图在「图」里写 `"自定义代码"`（可用 `ax`/`np`/`c()`，会标注未受约束验算）。`--dump-png out/` 落图供人眼与 judge 看；防重注用 `figure:*` 命名，重来加 `--replace`。扫描件走 `prep_assets.py` 的「扫描件」清洗而不是重画（除非要拆步骤/统一标签/做动画）。
9. **图标（规格书「图标」行声明了才做）** —— `python scripts/inject_icons.py deck.pptx icons.json --colors "INK=111111,ACCENT=E4002B"`：Tabler 线性图标（MIT，内嵌字体，离线可用、笔触天然统一），按**锚点形状名**定位（`{"图标":"安全","锚点":"能力卡一","位置":"内部上方居中","颜色":"ACCENT","尺寸":0.6}`），单色、每页 ≤3、禁 emoji、禁「图标+圆角卡片+投影」三件套。重复注入会被拦，重来加 `--replace`。锚点名就是施工时 `addShape/addText` 的 `objectName`（与动画同一套命名）。
10. **动画（规格书「动画」行被确认，或用户主动要了）** —— 按确认的方案做。菜单与脚本格式见 `scripts/inject_anim.py` 文档头：出现/淡入/浮入/飞入/缩放/擦入/强调-放大 7 种页内效果 + 淡入/推入/擦除/平滑 4 种切换（`平滑` 可给「选项」：按对象／按词／按字——那种「上一页的元素变成下一页的一部分、变换时带一点小模糊」的效果就是它，做法与边界见 `references/anim-atlas.md` 手法 1），按形状名（施工时给要动的形状设 `objectName`）定位，触发分 点击/同时/之后/自动。**分组就是触发语义**：`点击`/`自动` 开新组，`之后`/`同时` 并进上一组——"一次揭示出几个"＝"这一组里放几条"。deck 生成后先注入再过闸门：注入器自带 PowerPoint COM 验证（文件能打开 → 每条效果的类型/形状/触发/时长/缓动/节拍延迟与脚本逐条一致 → 方向与缓动编码往返保真），不全绿不算注入成功；注入报告会把**分组结构逐组打印**（组N 自动/点击：哪些形状、多长），照它核一遍你的节拍意图。样板页带动画时 PNG 看不到动画——把节拍逐页念给用户确认。**更隐蔽的推论**：动画位移**不改变形状的存储坐标**，所以预览图里运动主体永远停在起点，**动画终态从来没有被任何一道闸门看过**（qa 查的是存储坐标，judge 看的是静态图）。凡是让元素「走」到别处的页（尤其是手写位移），交付前**自己渲一张终态凭证帧**：把终态坐标写进一个副本再渲一帧，让终点也被人眼核过（模板见 `light-time-deck/endstate_render.py`）。**纪律（按重要性）**：① 先判档：**有人现场讲** → 按节拍分组揭示；**没人讲**（炫技/展映/用户明确要效果）→ 节拍约束整类豁免，长动画、组合动画、一页多组、整页自动播完放手做，但规格书写明、交付列出保留的告警。② 一页 ≤4 组，**组 = 一个论点单元**，不要单对象成组（同一句话里的东西应同一次出现；qa 会点出节拍过碎）。③ 单个节拍 ≤8s（这一次点击到下一次点击之间别让观众干等），单条 0.3–2s（短于 0.2s 看不见、长于 3s 拖沓）。④ 对象动画 ≤8 条/页——**一页要 10 条通常说明这页该拆页**，别指望靠分组把它点舒服。⑤ morph 的两页要有同名形状（否则整页退化成淡入，qa 会点名未配对的形状）。⑥ 缓动走默认「缓入缓出」（`出现`固定线性）。**②–④ 只属于"有人讲"档**；豁免不等于放弃交代，也不等于可以反过来胡来：**没有任何档位允许你为了报告好看而削弱动画**——把用户的炫技需求改平庸，比留下几条告警糟得多。**图片型形状同样能挂动画**：公式图、插图、图标按 `formula:<式子前 24 字>` / `figure:<标签>` / `icon:<图标名>` 这套派生名引用即可——但注入器按名定位且要求同页唯一，同一页用两次同一个图标会直接报「形状名出现多次」。**本技能不写自动换片时间**（不碰 `advTm`/`advClick`，也不动放映设置）——产物不会自己翻页，讲到一半跑掉那类现象是模板或别的工具加的，qa 会拦。**菜单外的效果（手写 XML 逃生舱，默认关闭）**：路径动画、旋转、3D 这类菜单没有的效果，**只有用户明确要求、且已明确同意之后**才能动这个口子——命令加 `--raw`，脚本里写 `"原始XML": {"<页>": {"XML": "<p:timing>…</p:timing>", "原因": "…", "用户已同意": true}}`，三样缺一即拒；不给 `--raw` 时脚本里出现 `原始XML` 直接报错。工具仍会在写入前拦掉悬空 spid、非 `<p:timing>` 根元素、slide 根元素没声明的命名空间前缀（这三类都会让 PowerPoint 静默丢弃动画）。**手写页默认只验「文件能打开 + 读回条数」**（报告标 `[降级]`、退出码 2，交付说明必须声明未经逐条验证）——但这次核对**可以补回来**：给这条原始XML 加一份 `"断言"` 清单（每条写 形状/presetID/起始ms/时长ms/重复/参数，**必须覆盖该页全部效果**，按页内出现顺序写），工具就逐条核对现场记录，并与 COM 读回的起始延迟 `Timing.TriggerDelayTime`、时长交叉核对；对上了就不再算降级。字段与例子见 `scripts/inject_anim.py` 文档头。默认一律走菜单：不要为了「更炫」主动去开这个口子，更不要拿它绕开预算纪律。想找更讲究的手法（morph 连续性、错峰节拍等）看 `references/anim-atlas.md`——那是取材库，不是必须遵守的规范。
11. **闸门一：程序化检查** —— `python scripts/qa.py <文件.pptx>`（**读退出码要在不带管道的那次调用上读**：`qa.py … | tail` 之后 `$?` 拿到的是 `tail` 的状态，真实退出码会被吞掉——这个坑实测踩过），修到干净再往下（退出码 0/1/2 ＝ 干净/有硬伤/仅告警或检查降级，见下方闸门一说明）；投屏场景加 `--min-pt 14`——预设字号体系里 12pt 的图注、脚注类要**同步提到 14pt 并写进规格书**，否则必被闸门一判掉；核对用色纪律可加 `--palette`（面积占比是近似参考——背景近似、文字色不计入，也**不含戳记/公式图等强调元素**，强调色预算要另目测——用来核对纪律的角色结构，不是精确对账工具）。
12. **闸门二：渲染视觉验收** —— 渲染成 PNG（多页加 `--contact` 出全 deck 拼图，deck 级评审一眼看穿骨架复读），派 judge 子代理逐页 + 全 deck 评审，prompt 按 `references/judge-prompt.md` 填空（deck 带动画时把动画脚本、带插图时把**作图依据**一并给 judge），按 verdict 修，然后**让同一个 judge 复核**（复核轮用 `--pages` 只重渲改动的页）。

用户中途改需求：先改规格书对应行，只重做受影响的页，不必推倒重来；若波及一句主张或风格本身，回到阶段二重新确认。交付之后的修改走同一条小循环：改代码 → 闸门一 → `render.py deck.pptx --pages <改动页>` 重渲 → judge 复核改动页；改动波及规格书里的项时先改规格书再动手。

---

## 两道闸门（都不能省）

你写代码画幻灯片，但你看不见成品。坐标算错、文字溢出、两个框叠在一起，代码都不会报错，文件照样生成。所以必须两道闸门，而且**它们抓的是完全不同的两类错**：

**闸门一 · 程序化检查**（`scripts/qa.py`）抓「断没断」：

- 几何/排版：文字高度超框（含表格单元格）、单行超宽（会静默重排）、文本框重叠、出画布、最小字号（自动缩字按实际渲染字号判）、图片拉伸变形、画布尺寸、自动缩字；**旋转形状 ↔ 文字的净空**（按矩形多边形做分离轴测试，报实测值：相交且压在文字之上、或净空 <0.10 英寸才告警，见 pitfalls #16）。宽度用 **Pillow 读真实字体度量**，不是估算——单一估算常数会同时造成误报和漏报（微软雅黑大写 A 是 0.70em、Arial Black 0.78em，而常用的估算常数是 0.55em）
- 中文排版：行首/行尾禁则、跨行劈词、孤行尾行、中文排进拉丁专用字体（渲染回退成衬线体，pitfalls #21）；纯拉丁文本这几项自动不命中，属预期
- **对比度**：文字与其下方纯色底的 WCAG 对比度（压图、图表=底色未知，跳过交闸门二）；run 没写颜色（继承主题近黑）压深底单独兜底
- 信息密度：每页字数当量粗查（拉丁字母按半字计；`--max-chars`，默认 300；投屏建议自降到 150）——它是建议阈值，用户要求保留的内容优先于建议值，放宽并在交付说明注明
- 交付合法性：OPC 包结构、单段落多个 `<a:pPr>`（PowerPoint 打开会错乱、LibreOffice 预览抓不到）、堆叠柱状图配 `outEnd` 标签（PowerPoint 报修复丢图表）、饼图超 5 块、颜色值带 `#`、硬/柔阴影混用（pitfalls #2 的互掩护变体）、关系指向空 part
- 表格外框 vs 内容：`a:ext@cy` 与 `Σ a:tr@h`、`a:ext@cx` 与 `Σ a:gridCol@w` 对不上就判硬伤——pptxgenjs 的 `addTable` 缺 `h` 时外框退化成 1 英寸、缺 `w` 时退化成 75% 页宽，真 PowerPoint 会裁掉超出部分，而 WPS/LibreOffice 自动撑开、**渲染预览 100% 看不见**（pitfalls #30）
- 字体可移植性：跨平台安全名单核对（含 macOS 苹方/黑体-简与日韩系统字体）——只核 Windows 自带不够，deck 发给 Mac 用户一样会被替换
- deck 级：连续同页眉骨架、全宽色带（页眉页脚都查）、连续无图无图表的载体断档——qa 只认图片/图表对象，若版面的载体是流程示意、大数字等（styles.md §6.5 的口径），此告警可声明豁免，交付时说明即可
- 公式残留：未转换的 LaTeX（`$$…$$` 或带 `\` `^` `_` 的 `$…$`）会显示为乱码文本，判硬伤——先跑 inject_math.py 再交付
- 图片有效 PPI：裁切后像素 ÷ 放置英寸，<72 判硬伤（必糊）、<150 告警——照片/公式/图标/插图一视同仁；注：inject_math/inject_icons/inject_figure 产出的图天然 600–800，不会误报
- 图片型图形对比度：`icon:*` / `formula:*` / `figure:*` 命名的图片按**墨色（最暗一档）** vs 所在底色兜底（<2.5 硬伤、<3.0 告警）——图片不进文字对比度检查，深底上的暗色强调（红公式压蓝图蓝、深线图上深底）靠这条拦（pitfalls #24）
- 插图几何：`figure:*` 的有效 PPI 与对比度同上兜底；**几何约束在 inject_figure.py 注入时已逐条验算**（超差不写入文件），qa 不判断几何对错——图是否忠实题面交闸门二对照「作图依据」审（pitfalls #25）
- 图标与 emoji：每页 `icon:*` 图片 >3 个告警（图标是路标不是装饰）；正文里出现 emoji 告警（反 AI 清单禁 emoji 当图标）
- 正文里的几何/数学符号：`△ □ ○ ∠ ⊥ ∥` 这类告警——中文字体把它们画成汉字的四成高、笔画细一档，看着就是 LLM 文本味（pitfalls #29）；句子里写汉字、数学式走 `inject_math`、图形用 `inject_figure`。`①`、`½` 是满尺寸的，不在此列
- 动画纪律（注入动画后）：动画引用不存在的形状 id 判硬伤（会提示修复或静默丢弃）；**节拍护栏**——一页揭示节拍 >4 组告警（演讲时手上太忙）、单对象成组（节拍过碎，同一句话里的东西该同一次出现）告警、单个节拍 >8s 告警（这一次点击后观众干等）、单条时长 <0.2s（看不见）或 >3s（拖沓）告警、对象动画 >8 条/页告警；morph 两页无同名形状告警（会整页退化成淡入，报告点名两页各自独有的形状）；**自动换片风险**告警——某页带 `advTm`/`advClick="0"`，或演示文稿开了「使用计时」(`p:showPr@useTimings`)、放映方式是展台循环 (`showType="kiosk"`)，这些会让幻灯片自己翻过去、讲到一半跑掉（`p14:dur` 是切换本身的播放时长，不是换片计时）。**qa 不预设"一页几组"**：分组由内容决定，护栏只拦明显不合理的

检查结果按 `[S页码]` 标注，多页时逐页看，不会跨页配对。报告分三类：**硬伤**（必修）、**告警**（建议，可声明豁免）、**检查降级**——某类检查因环境缺失整类没跑（jieba 未装致劈词检查关闭、Pillow 缺席或非 Windows 无字体目录致宽度只能用估算）。**降级不等于通过**：报告顶部会单列成块、计入退出码 2，交付说明里必须声明哪类检查没执行，不得声称该项已通过。**告警不是"必须清零"**：节拍与预算那几条（>4 组、单对象成组、单节拍 >8s、>8 条/页）在**炫技/展映档**下可明知而保留，规格书里写明、交付说明里列出即可；**任何档位都不许为了让报告干净而削弱动画**——把炫技需求改成平庸比留下几条告警糟得多。

**闸门二 · 视觉验收**（judge 子代理）抓「好不好看」：对比度之外的层次、焦点、留白节奏、观感是否地道。

两者不可替代：库级 bug（比如阴影模糊被悄悄改掉）只有读 XML 能发现；中文断错词只有看图或查断点能发现。**程序化检查干净只证明没断，不证明能看。**

**闸门二的前提与盲区（必读）**：闸门二成立的前提是「渲染器能忠实还原 PowerPoint」。当渲染回退到 **WPS / LibreOffice**（脚本会打印应用身份，`12.x` 即 WPS 兼容层），有一类结构性错误**渲染出来一定正常、真 PowerPoint 打开才坏**——最典型的是**表格外框裁剪**（pitfalls #30：WPS/LO 会按内容自动撑开表格，把过小的外框抹平）、**字体静默替换**（pitfalls #1）、**动画 timing 被丢弃**。这类问题**必须靠闸门一的结构检查或直接读 XML 兜底，不能以「预览通过」结案**。遇到验收只差这一步时，宁可多看一遍 XML，也不要相信渲染图。

### 派 judge 的硬要求（prompt 按 `references/judge-prompt.md` 填空，不要现场即兴）

1. **先渲染成 PNG 再交给 judge**，把图片路径给它。judge 只能读预渲染的 PNG，不能直接开 pptx。
2. **划定作用域**：告诉 judge 几何/字号/对比度/信息密度已由闸门一用**真实字体度量**核过、**不要复测**（它按像素估字号有系统偏差，会给出「看着专业的错数」）；它的活是跨页一致性、基线对齐、层次焦点、观感、动效与版面是否打架。**指定一个临时目录**给它写裁图，交付目录 `preview/` 不许污染；允许它读施工源码核对声明，但要求把「来自源码核对」与「图里看见的」分开写。
2. **必须在 prompt 里声明本 deck 刻意违反了哪些通用规则**，否则 judge 会把风格特征当缺陷报回来。例：新粗野主义的粗黑描边、零模糊硬阴影、高饱和撞色、刻意旋转、无圆角是**定义性特征，不是缺陷**，反倒是柔和阴影才是失败。模板会带齐设计意图（配色纪律、字体字号、论证结构）、deck 级检查清单（版式轮换、强调色预算、载体轮换、亮点唯一性、来源忠实度、反 AI 清单）和输出格式。

judge 判 fail 时，逐条修完再让**同一个 judge** 复核（它有上一轮上下文，复核更准）。**但轮次本身就是最大的成本项**：复核轮要把整段 prompt 加全部历史重发一遍，实测 5 轮合计约 400 万 token，其中最后一轮只调了 3 次工具、只读 1 张图，仍花 22.6 万——**成本几乎只由轮次数决定，不由图大小决定**。所以：把能自己发现的都改掉再派复核；多页 deck 先只送拼图做一轮分诊，再对可疑页放大（详见上面那条）。不要自己看图代替 judge，也不要在派 judge 之前先看一遍——那是重复评审，白烧一次渲染。

**没有子代理通道的会话**（比如你本身就是被派出来的子代理，再派不出下一层）：按 `references/judge-prompt.md` 填空后**自己逐条执行**这份清单代替 judge，并在交付说明里如实声明「闸门二为降级自检，缺独立 judge」。降级自检抓执行缺陷仍然有效（实测抓过十字马克压字），但缺新鲜眼光——不要默不作声地跳过闸门二。降级模式下**必须自己看渲染图**——这与上文「不要自己看图代替 judge」不冲突：那条约束的是有 judge 通道的时候。

这次诊断出的真实教训（已写入 styles.md 页级编排）：一份 10 页 deck 可以配色完全正确、文案好听、无一处溢出，但连续 8 页共用同一骨架 + 每页一条全宽页脚带 + 强调色洒满全场 + 零图片零图表——几何检查全过，成品却是标准 AI 脸。门一保证「没坏」，门二必须审「像不像人排的」。

---

## 附带脚本

| 脚本 | 用途 | 用法 |
|---|---|---|
| `scripts/qa.py` | 闸门一：几何 / 中文排版 / 对比度 / 信息密度 / 交付合法性（含表格外框裁剪）/ 动画纪律 / 字体可移植性 / deck 级骨架·色带·载体断档 | `python qa.py deck.pptx [--min-pt 14] [--max-chars 300] [--palette]`；退出码 0/1/2＝干净/硬伤/仅告警或有检查降级（报告分硬伤·告警·降级三类，降级见闸门一说明） |
| `scripts/render.py` | 渲染 PNG（PowerPoint COM 优先，LibreOffice 兜底）；`--pages 2-4,7` 只渲染指定页，`--contact` 另出全 deck 拼图 | `python render.py deck.pptx [tag] [--pages 2] [--contact]` |
| `scripts/skeleton.js` | 起手模板：栅格常量、调色板、已绕过 bug 的辅助函数 | 复制后改 |
| `scripts/inject_anim.py` | 动画注入：中文菜单脚本编译成 PowerPoint 权威 XML（菜单/脚本格式见文件头），注入后 COM 逐条验证（类型/形状/触发/时长/缓动/节拍延迟）并打印分组结构；`--raw` 是菜单外效果的手写 XML 逃生舱（须用户同意，逐条断言关闭＝`[降级]`） | `python inject_anim.py deck.pptx anim.json`（重注入加 `--replace`；逃生舱加 `--raw`） |
| `scripts/inject_math.py` | 公式：块级 LaTeX（`$$…$$` 独占文本框）渲染为 600dpi 透明图放回原位，字号/颜色自动继承，WPS 与 PowerPoint 观感一致 | `python inject_math.py deck.pptx [--dpi 1200] [--fontset stix]` |
| `scripts/inject_icons.py` | 图标：Tabler 线性（MIT 内嵌字体，离线、笔触统一），按锚点形状定位、单色、防重注 | `python inject_icons.py deck.pptx icons.json --colors "INK=111111,ACCENT=E4002B"`（重来加 `--replace`） |
| `scripts/inject_figure.py` | 插图：中文声明式规格编译成函数图像 / 平面几何图（600dpi 透明图，按锚点落位），注入时**逐条验算几何约束**，词表外的怪图有「自定义代码」逃生舱 | `python inject_figure.py deck.pptx figures.json --colors "INK=111111,ACCENT=E4002B"`（重来加 `--replace`；`--dump-png out/` 落图） |
| `scripts/prep_assets.py` | 素材：先清点回报（尺寸/比例/问题），再按规格无损调理（EXIF 转正 / sRGB / 裁切 / 300PPI）；**「扫描件」清洗**（白底/去斜/裁白边/对比，治手机拍卷子）；双色调等要了才做 | `python prep_assets.py assets/` 清点；`python prep_assets.py assets/ spec.json` 处理 |
| `scripts/selftest.py` | qa 回归自测：临时造带病 / 小画布 / 干净三份 deck，断言每类检查命中与干净稿放行 | `python scripts/selftest.py`（改 qa.py 后必跑；干净稿需要 cwd 有 node_modules 祖先链，缺了会跳过并注明） |

脚本按需复制到工作目录再改，不要直接在技能目录里改。

**依赖**（本机已装；在别的机器上先用这些命令装齐）：

```bash
npm install pptxgenjs              # 生成 pptx
python -m pip install python-pptx  # qa.py 读结构（硬依赖，缺了装不上就跑不了闸门一）
python -m pip install pywin32      # render.py 用 PowerPoint COM 导 PNG（Windows）
python -m pip install pillow       # qa.py 读真实字体度量；缺失则回落估算并在报告里标注「检查降级」
python -m pip install defusedxml   # qa.py 安全解析 pptx 内的 XML
python -m pip install jieba        # qa.py 的劈词检测；不装则劈词检查关闭并在报告里标注「检查降级」（不再静默）
python -m pip install matplotlib   # inject_math 渲染公式、inject_figure 画插图（自带 numpy；prep_assets 的去斜依赖 numpy，缺了自动跳过并提示）
python -m pip install pillow-heif  # prep_assets.py 读 iPhone 的 HEIC（可选；不装则提示导出 JPG）
```

图标字体随技能内嵌在 `assets/icons/`（Tabler Icons，MIT，Copyright (c) 2020-2026 Paweł Kuna；许可证见同目录 LICENSE-tabler-icons.txt），无需联网安装。

qa.py 的真实字体度量目前映射 Windows 字体目录；在 macOS / Linux 上自动回落估算并单独告警，渲染走 LibreOffice 路线。装不上 pywin32 时，render.py 自动回退 LibreOffice + poppler-utils。动画验证（inject_anim.py 的 COM 三层校验）同样依赖 PowerPoint COM：无 COM 的机器注入后无法自动验证，脚本会如实告警，此时不要声称动画已验证。

---

## 交付时说什么

说清四件事，别只丢一个文件路径：

1. **文件在哪**，预览图在哪。
2. **这一页/这一份长什么样** —— 用两三句描述实际视觉效果，让用户不打开也知道自己拿到了什么。带动画的 deck **逐页报节拍**：这一页几组、每组是哪些元素、要点几次（例：「P3 三组：①工程图框自动起播 ②三个问题点一次全出 ③结论块再点一次」），并说清哪些页是整页自动播完、不用点。动效质量一句话带过即可（缓动走的是默认档）；节拍要过用户耳朵，因为**点击数是他演讲时的手感**。
3. **过程中修掉了什么真问题** —— 尤其是 qa 与 judge 判出后改的那些。这既是诚实交代，也让用户知道成品是被检查过的。
4. **对照清单自检结果** —— 交付前最后一遍：那一句主张有没有真的被这些页撑起来？每页标题是不是判断句？风格是否忠实来源、用色纪律有没有守住（可贴 `--palette` 报告佐证）？效果承诺是否兑现（翻开 1 秒落点是否唯一）？有没有命中反 AI 审美清单里任何一条批量默认（含 emoji 当图标、图标家族混用、罐头示意图）？插图**逐张交代作图依据与约束验算结果**（全部通过／哪条超差被 `--force` 放行／哪张走了自定义代码未受约束验算）；图片素材**逐张列出出处**（用户提供／外部来源，外部素材必须能说出处）；闸门一输出若含**「检查降级」**，如实说明哪类检查没执行（jieba 不在就劈词没查、非 Windows 字体度量是估算）——**不得声称该项已通过**；用动画逃生舱（`--raw` 手写 XML）的页：**附了「断言」并逐条核对通过的不算降级**（说一句「手写页已按断言清单逐条核对」即可）；没附断言的仍算降级——注入器只验证「文件能打开 + 读回条数」，必须如实说明这部分动画未经逐条验证。**动画还要交代播放体验**：哪些页是视觉型（整页自动播完、不用点）、哪些是讲解型（几组点击、每组几个），以及「这份不会自己翻页」（qa 若报自动换片风险，先修掉再交付）；节拍也要写进演讲备注，让演讲者视图能看见。没有违例就说"已对照，无违例"。

如果有为了风格而做的取舍（比如低对比度是风格要求、某个数字是估算），主动说明，不要藏。
